let numeros = [1,2,3,4,5,6,7]
// for 
for (let i = 0; i < numeros.length; i++) {
    const element = numeros[i] ;
    console.log(element);
}

let numeros1 = [1,2,3,4,5,6,7];
let suma = 0;
for (let i = 0; i < numeros1.length; i++) {
    suma = numeros1[i] + suma;
    console.log(numeros1[i]);
}
console.log('suma  :',suma);
// [1,4,9,16,25]  ver el push
//let newArreglo=[]

// estudiar el while y do while  FOR CON LET  FOR (LET OF COMIDAS)

// break sirve para romper un bucle


